fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0
python-multipart==0.0.20
httpx==0.27.0
openai==1.58.1
weasyprint==63.0
jinja2==3.1.5
openpyxl==3.1.5
sqlalchemy==2.0.36
greenlet==3.5.3
aiosqlite==0.20.0
asyncpg==0.30.0
supabase==2.10.0
python-dotenv==1.0.1
# Also remove duplicate MODEL_EMBEDDING at top if we now have them in the middle - check file
pypdf==5.1.0
pypdfium2>=4.30.0
pymupdf>=1.24.0
langchain-text-splitters==0.3.4
langchain-core==0.3.28
# PaddleOCR — in-process Stage-1 image OCR (lang=ch: 简体中文 + English)
paddlepaddle>=3.3.0
paddleocr>=3.7.0
# PaddleOCR wants numpy<2.4; MinerU may pull newer — pin after mineru install:
numpy>=1.24,<2.4
pillow>=10.0.0
# Optional Stage-1 PDF OCR (heavy): pip install -U "mineru[core]"
# then: python -m mineru.cli.models_download -s huggingface -m pipeline
pytest==8.3.4
pytest-asyncio==0.24.0
